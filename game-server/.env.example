# Game Server environment variables
# KHÔNG commit file này — add .env to .gitignore

# Sui configuration (sau khi deploy contracts Phase 0)
ONEREALM_PACKAGE_ID=0x_REPLACE_WITH_PACKAGE_ID_FROM_DEPLOY
SUI_RPC_URL=https://fullnode.devnet.sui.io

# Sponsor wallet (ADR-008: self-managed, faucet-funded keypair)
# Generate: sui client new-address ed25519 → copy secret key
SPONSOR_PRIVATE_KEY=REPLACE_WITH_SPONSOR_PRIVATE_KEY_HEX
SPONSOR_ADDRESS=REPLACE_WITH_SPONSOR_ADDRESS

# Server config
PORT=3001
